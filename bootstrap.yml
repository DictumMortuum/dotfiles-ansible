- hosts: all
  become: yes
  roles:
    - system/setup-skel
    - system/setup-dependencies
    - { role: system/setup-zerotier, network_id: "83048a06326833ff" }
    - { role: system/setup-known-hosts, keyscan_host: "satellite.dictummortuum.com" }
    - { role: system/setup-known-hosts, keyscan_host: "github.com" }
    - { role: system/setup-known-hosts, keyscan_host: "gitlab.com" }

- hosts: desktop
  become: yes
  roles:
    - system/setup-ldap-client

- hosts: all
  become: yes
  roles:
    - system/setup-thirdparty

- hosts: all
  become: yes
  become_user: dimitris
  roles:
    - user/setup-keys
    - user/setup-dotfiles

- hosts: all
  become: yes
  become_user: consul
  roles:
    - user/setup-keys
    - user/setup-dotfiles
